language: rust

script:
     - cargo install cargo-vcpkg
     - cargo vcpkg build
     - cargo test --release 
     
jobs:
    include:        
        - dist : xenial
        - os : windows 
          